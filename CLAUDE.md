## Workflow
- When fixing bugs, verify each fix live in the browser before committing.
- Commit each fix individually as it's confirmed working — never batch
  multiple unrelated fixes into one commit.
